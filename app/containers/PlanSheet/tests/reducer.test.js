/* import expect from 'expect';
import planSheetReducer from '../reducer';
import { fromJS } from 'immutable';

describe('planSheetReducer', () => {
  it('returns the initial state', () => {
    expect(planSheetReducer(undefined, {})).toEqual(fromJS({}));
  });
}); */
